<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC000000">

<com.google.android.material.card.MaterialCardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="24dp"
    android:maxWidth="520dp"
    app:cardCornerRadius="28dp"
    app:cardElevation="32dp"
    app:cardBackgroundColor="#FF0000"
    app:strokeColor="#4000FF88"
    app:strokeWidth="1dp">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="32dp">

        <!-- Botón cerrar -->
        <ImageView
            android:id="@+id/btnClose"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:contentDescription="Cerrar"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="@color/white"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="6dp"/>

        <!-- Título -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="NEQUI KILL V5.5"
            android:textColor="#FFFFFFFF"
            android:textSize="26sp"
            android:fontFamily="@font/manrope_bold"
            android:gravity="start"
            android:lineSpacingExtra="4dp"
            android:shadowColor="#8000FF88"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="8"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/tvBody"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Bienvenido a la plataforma más realista de Nequi. Únete ahora para explorar funciones avanzadas y recibir soporte oficial."
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:fontFamily="@font/manrope_medium"
            android:gravity="start"
            android:lineSpacingExtra="6dp"
            app:layout_constraintTop_toBottomOf="@id/tvTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Botones -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTelegramContact"
            android:layout_width="0dp"
            android:layout_height="58dp"
            android:layout_marginTop="24dp"
            android:text="Adquiere tu acceso ahora"
            android:textAllCaps="false"
            android:textSize="16sp"
            android:fontFamily="@font/manrope_bold"
            android:textColor="#FFFFFF"
            app:backgroundTint="#FFEB3B"
            app:cornerRadius="14dp"
            app:elevation="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvBody"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnJoinGroup"
            android:layout_width="0dp"
            android:layout_height="54dp"
            android:layout_marginTop="12dp"
            android:text="Grupo Oficial"
            android:textAllCaps="false"
            android:textSize="15sp"
            android:fontFamily="@font/manrope_medium"
            android:textColor="#FF0000"
            app:backgroundTint="#FFFFFF"
            app:cornerRadius="14dp"
            app:strokeColor="#4000FF88"
            app:strokeWidth="1dp"
            app:layout_constraintTop_toBottomOf="@id/btnTelegramContact"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- VERSIÓN -->
        <TextView
            android:id="@+id/tvVersion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Versión v5.5"
            android:textSize="11sp"
            android:textColor="#B3FFFFFF"
            android:fontFamily="@font/manrope_medium"
            app:layout_constraintTop_toBottomOf="@id/btnJoinGroup"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:gravity="center"
            android:layout_gravity="center_horizontal" />

    </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</com.google.android.material.card.MaterialCardView>
</FrameLayout>
